# “搁这儿”产品需求文档（MVP）

> 英文工作名：**ForNow**  
> 产品口号：**文件、文字、链接，先搁这儿。**

## 1. 产品概述

“搁这儿”是一款 macOS 轻量暂存工具。它把 MacBook 屏幕顶部的 Notch（刘海）区域变成一个随手可用的临时口袋：用户可以把文件拖到 Notch 中，也可以点击 Notch 打开面板，再通过 `Command + V` 放入剪贴板中的文字、图片、文件或链接。

产品只解决一件事：**让用户在尚未决定内容最终放在哪里时，先拥有一个触手可及的中转位置。**

## 2. 用户问题

用户在跨窗口、跨应用处理内容时，经常遇到以下情况：

- 拖动文件时，需要先松手、切换窗口，再重新找到文件。
- 暂时需要保存一段文字或链接，但不值得专门创建笔记。
- 从多个位置收集素材后，希望一次性拖入邮件、聊天、文档或文件夹。
- 桌面和下载目录容易被“稍后处理”的临时内容占满。

## 3. 目标用户

- 经常在 Finder、浏览器、聊天工具和办公软件之间传递内容的用户。
- 需要收集图片、文档、素材的设计师、内容创作者和产品经理。
- 经常向 ChatGPT、Claude、邮件或 Teams 中上传多个文件的用户。

## 4. 核心使用场景

### 场景 A：拖入暂存

1. 用户开始拖动一个或多个文件。
2. 指针进入 Notch 区域后，Notch 向下展开并显示可放置状态。
3. 用户松开鼠标，文件进入暂存站。
4. 面板短暂显示成功反馈后自动收起。

### 场景 B：粘贴剪贴板内容

1. 用户点击 Notch，打开暂存面板。
2. 用户按下 `Command + V`。
3. 应用识别剪贴板内容类型，并创建对应的暂存项目：文字、链接、图片或文件。
4. 新项目出现在列表顶部。

### 场景 C：取出并继续使用

1. 用户点击 Notch 打开暂存面板。
2. 用户将一个或多个项目拖出。
3. 项目可被放入 Finder、邮件、聊天窗口、浏览器上传区或其他支持拖放的应用。
4. 拖出后默认保留项目，避免误操作导致内容丢失；用户可手动删除。

## 5. MVP 功能需求

### 5.1 Notch 触发

- 点击 Notch 区域，展开或收起暂存面板。
- 拖动内容靠近 Notch 时，自动展开面板。
- 点击面板外部或按下 `Esc`，收起面板。
- 全屏应用中是否启用可由用户设置，默认启用。

### 5.2 支持的内容

| 内容类型 | 加入方式 | 展示方式 | 取出方式 |
| --- | --- | --- | --- |
| 文件/文件夹 | 拖入、粘贴 | 图标、名称、大小 | 拖出、复制、在 Finder 中显示 |
| 图片 | 拖入、粘贴 | 缩略图、尺寸 | 拖出、复制、快速预览 |
| 文字 | 粘贴 | 前两行摘要 | 复制、拖出为文本 |
| 链接 | 粘贴、拖入 | 网页标题或域名、URL | 点击打开、复制、拖出 |

同一批加入的多个项目应保持原有顺序。

### 5.3 暂存面板

- 默认采用紧凑的纵向列表或卡片布局。
- 每个项目显示类型、名称或摘要、加入时间。
- 支持单选、多选和全选。
- 支持快速预览文件和图片。
- 支持以下项目操作：复制、打开、在 Finder 中显示、删除。
- 提供“清空全部”，执行前需要二次确认。
- 显示当前项目数量和占用空间。

### 5.4 数据保存

- 所有内容仅保存在本机，不上传云端。
- 文件默认复制到应用管理的暂存目录，避免原文件被移动或删除后项目失效。
- 文本和链接保存在本地数据库中。
- 退出应用或重启 Mac 后，暂存内容仍然存在。
- MVP 默认由用户手动删除，不自动清理。

### 5.5 菜单栏与设置

- 提供菜单栏图标，作为无法点击 Notch 时的备用入口。
- 基础设置包括：登录时启动、全屏时启用、声音反馈、动画效果。
- 支持自定义全局快捷键，默认建议为 `Control + Option + Space`。

## 6. 关键交互原则

- **快速：** 从触发到面板可用应尽量控制在 200 ms 左右。
- **可预期：** 拖出项目不会默认删除，删除必须由用户明确执行。
- **低打扰：** 不主动读取或记录全部剪贴板历史，只有用户打开面板并粘贴时才保存内容。
- **有反馈：** 可放置、放置成功、复制成功和删除均应提供清晰但克制的视觉反馈。
- **符合 macOS：** 支持深色模式、减少动态效果、键盘操作和 VoiceOver。

## 7. 边界与异常处理

- 重名文件进入暂存目录时，应用自动生成唯一内部名称，界面仍显示原文件名。
- 文件复制尚未完成时显示进度，不允许将不完整项目拖出。
- 原文件不可读取、磁盘空间不足或格式不支持时，给出明确错误提示。
- 剪贴板包含多种表示形式时，优先级为：文件 > 图片 > 链接 > 富文本 > 纯文本，并允许后续调整。
- 外接显示器没有 Notch 时，通过菜单栏图标、快捷键或屏幕顶部中央热区打开。

## 8. 非 MVP 范围

首版暂不包含：

- 跨设备或云端同步。
- 完整的剪贴板历史记录。
- 标签、文件夹和复杂分类。
- AI 摘要或内容识别。
- 文件上传、分享链接、压缩和格式转换。
- 多个独立暂存架。

## 9. 后续版本方向

- 自动清理策略：使用后删除、24 小时、7 天或自定义时间。
- 固定重要项目，自动清理时跳过。
- 按应用或任务创建多个暂存架。
- 最近关闭的暂存内容恢复。
- Quick Actions：压缩、重命名、转换图片、生成分享链接。
- iCloud 同步或端到端加密同步。

## 10. MVP 验收标准

- 用户能通过点击 Notch 或快捷键打开面板。
- 文件拖到 Notch 后能够被可靠保存，并可拖入其他应用。
- 用户能通过 `Command + V` 暂存文字、链接、图片和文件。
- 暂存内容在应用重启和系统重启后仍然存在。
- 用户能预览、复制、打开、删除和清空项目。
- 所有核心功能无需联网，用户内容不会离开本机。
- 没有 Notch 或使用外接显示器时，仍存在可用的备用入口。

## 11. 名称建议

### 主推：搁这儿（ForNow）

“搁这儿”像用户在工作中自然说出的一句话，轻松、有记忆点，也准确表达“暂时放下，之后再处理”的产品价值。它不会把产品限制在文件管理上，未来加入文字、链接、图片和更多内容类型仍然成立。

建议口号：**文件、文字、链接，先搁这儿。**

### 备选名称

| 名称 | 感觉 | 说明 |
| --- | --- | --- |
| 先放这 | 最直白、亲切 | 一眼理解用途，适合中文市场 |
| 刘海兜 | 可爱、具象 | 强调 Notch 像一个顶部小口袋 |
| TopPouch | 轻巧、国际化 | 强调位于屏幕顶部的内容口袋 |
| Pitstop | 有速度感 | 内容在前往最终目的地前短暂停靠 |
| Layover | 克制、现代 | 表达内容的短暂停留和中转 |

> 注：正式上架前仍需进一步核验 App Store 名称、商标和域名可用性。

